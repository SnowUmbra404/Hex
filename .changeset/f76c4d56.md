---
"hex-app": patch
---

Lower event-handling overhead and cut background permission checks: the event tap callback now reads lock-protected handler snapshots instead of queue hops, the permission watchdog polls at 1s with a 100ms burst after system transitions, and the pointer monitor reuses a cached screen list instead of re-fetching it on every mouse move (hotkey and overlay-window recovery after wake are unchanged).
